const config = {
  title: "Apparaître dans Google AI Overview : le guide complet 2026",

  /** Maillage vers la page de définition, qui porte le « c'est quoi ». */
  links: [
    [
      "notre décryptage de Google AI Overview reprend la fonctionnalité depuis le début",
      "notre [[lien:google-ai-overview-definition|décryptage de Google AI Overview]] reprend la fonctionnalité depuis le début",
    ],
  ],

  /**
   * Le lien vers Triaina est un lien commercial vers l'éditrice du journal :
   * il reste dans le corps, signalé et en `nofollow`, mais il n'a pas sa place
   * dans le relevé de sources repris en `citation`.
   */
  excludeSources: ["triaina.fr"],
};

export default config;
